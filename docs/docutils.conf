[general]
# Citations cited once render their label as a backlink, while those cited several times
# render a plain label plus a ``(1,2,3)`` list of backlinks. Drop backlinks altogether so
# every bibliography entry looks the same.
footnote_backlinks: 0
