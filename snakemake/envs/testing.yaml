name: testing-fastdfe
channels:
  - conda-forge
dependencies:
  - dadi
  - pip
  - pip:
    - pytest
    - pytest-xdist
    - pytest-cov
    - pytest-sugar
    - pytest-pycharm
    - -r requirements_testing.txt