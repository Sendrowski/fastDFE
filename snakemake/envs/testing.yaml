name: testing-fastdfe
channels:
  - conda-forge
dependencies:
  - dadi
  - pip
  - pip:
    - pytest
    - pytest-xdist
    - pytest-cov
    - pytest-sugar
    - pytest-pycharm
    - pytest-timeout
    - -r requirements_testing.txt